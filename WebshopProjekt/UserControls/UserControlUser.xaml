<UserControl x:Class="WebshopProjekt.UserControls.UserControlUser"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WebshopProjekt.UserControls"
             mc:Ignorable="d" 
             Width="1100" 
             Height="700">
    <StackPanel Margin="0">
        <DataGrid
            x:Name="datagridUsers"
            Width="1100"
            Height="400"
            AutoGenerateColumns="False"
            CanUserAddRows="False"
            CanUserDeleteRows="False"
            IsReadOnly="True"
            SelectionChanged="datagridUsers_SelectionChanged">   
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Id}" Header="ID" />
                <DataGridTextColumn Binding="{Binding Username}" Header="Felhasználónév" />
                <DataGridTextColumn Binding="{Binding Password}" Header="Jelszó" />
                <DataGridTextColumn Binding="{Binding Email}" Header="Email" />
                <DataGridTextColumn Binding="{Binding Role}" Header="Szerepkör" />
            </DataGrid.Columns>
        </DataGrid>
        <WrapPanel Margin="10" VerticalAlignment="Bottom">
            <StackPanel Width="300">
                <TextBlock Text="Felhasználónév:" />
                <TextBox x:Name="UsernameTxtBx" />
                <TextBlock Text="Jelszó:" />
                <PasswordBox x:Name="PasswordBx" />
                <TextBlock Text="Email:" />
                <TextBox x:Name="EmailTxtBx" />
                <TextBlock Text="Szerepkör:" />
                <ComboBox x:Name="szerepkorBx" />
            </StackPanel>
            <StackPanel>
                <Button
            x:Name="mentesBtn"
            Width="200"
            Margin="20,10,0,0"
            Background="DarkSlateBlue"
            Click="mentesBtn_Click"
            Content="Mentés"
            Foreground="White"/>
                <Button
            x:Name="torlesBtn"
            Width="200"
            Margin="20,10,0,0"
            Background="Red"
            Click="torlesBtn_Click"
            Content="Törlés"
            Foreground="White" />
                <Button
            x:Name="modBtn"
            Width="200"
            Margin="20,10,0,0"
            Background="LightGoldenrodYellow"
            Click="modBtn_Click"
            Content="Módosítás" />

            </StackPanel>
        </WrapPanel>
    </StackPanel>
</UserControl>
